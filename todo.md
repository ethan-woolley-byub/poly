- highlight context text
- save context text in cookies

- account login
- save phrases and words

- don't requery context to gemini if already queried
- only querey context to gemini after adding a new language if the context button had already been clicked for that translation
- verify that context disapears if you translate something else unless the translation is very similar
- update textboxes after a delayed timeout
- spaced repition quiz
- save info from spaced repition
- use local ai and or local translation

- context elements line up
- no initial query if the text is already stored in a cookie    

new todo:
context tab
use deepl
    or let switch translatin provider
audio reading
listen to voice and translate
- escape key to exit add language modal (back select on phone)
export history and saved langauges

quiz feature


now we are going to add support for deepl api
I want a toggle button between google translate and deeplang api I want its value stored in a cookie. put this button in the nav bar. when you click it it should move to the other api and retranlsate the textareas from the source lang. Make deepl api the default

my api key for deepl is in the .env file

here is the reference for the deepl documentation
https://developers.deepl.com/api-reference


for the context tab add an indicator to show that we are on that tab

favicon

fix text to speech


0a7daf
no sports blue
no changes to shaka
light text to light instead of just white
dark should be used as dark background
dark medium should be secondary dark
